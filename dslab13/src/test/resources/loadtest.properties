# Number of concurrent download clients
clients=3

# Number of initiated uploads per client per minute
uploadsPerMin=100

# Number of initiated downloads per client per minute
downloadsPerMin=100

# Size of files available for download, in KiloBytes (simply generate test files with random content)
fileSizeKB=1000

# average ratio of uploads which overwrite an existing file (updating its version etc), as opposed to uploads which
# create a new file. For instance, overwriteRatio=0.8 means that 80% of uploads in the test environment overwrite an existing file.
overwriteRatio=80